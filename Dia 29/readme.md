# Repaso de funciones

```JS

fuction suma(a,b){
    let resultado =a+b; 
    return resultado;
}
console.log(suma(5,3));
const res= suma(24,-3);


//--- Funciones anonimas
const cuadrado= fuction (a){
    return a*a;
}

const res = cuadrado(3);
let res2= cudrado(5);




//-- Funciones flecha

const cubo= (a) =>{
    return a*a*a;
}
 const res3= cubo(3);



// Funcion felcha con 1 solo parametro y sin return
//recibe un numero  y devuelve el doble de ese numero.

const doble =(a) =>{
    return a*2;
}
```